server:
  port: 8080
  servlet:
    context-path:
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/gara_oto"
    username: root
    password: 10082000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
